PySide6-Essentials>=6.7
psutil>=6.0
pyqtgraph>=0.13.7
# Lets the update check verify TLS the way Windows does. Corporate proxies
# that inspect HTTPS install their own root CA, and those are often not
# standards-perfect enough for OpenSSL even though Windows accepts them.
truststore>=0.10; python_version >= "3.10"
